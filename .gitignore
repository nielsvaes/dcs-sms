# Worktrees
.worktrees/

# Superpowers brainstorm companion (mockups, server state)
.superpowers/

# Implementation plans are kept locally, not committed (specs are committed)
docs/superpowers/plans/

# Per-session Claude handoff snapshots — local-only orientation notes
.claude-handoff-*.md

# Claude Code per-project config — local-only by default; track only the
# project-shared slash commands under .claude/commands/.
.claude/*
!.claude/commands/

# Go build artifacts
tools/dcs-sms
tools/dcs-sms.exe
tools/dcs-sms.exe~
tools/**/*.test
tools/**/*.out

# The LuaSec payload binaries are intentionally vendored so install-me-mod can
# bundle them — override the global *.dll ignore for this folder only.
!tools/me-mod/luasec/payload/**/*.dll

# IDE
.idea/
.vscode/*
!.vscode/sms.code-snippets
*.swp

# OS
Thumbs.db
.DS_Store
